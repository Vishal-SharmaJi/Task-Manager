- [ ] Add 4 example recent activities to Dashboard (visible to all) in a way that never replaces real activity entirely
- [ ] Update server controller `getRecentActivity` to always include 4 example items
- [ ] Ensure response keeps UI-compatible fields: _id, title, status, updatedAt
- [ ] Run server/client checks (start server or hit endpoint) and verify dashboard shows example items

